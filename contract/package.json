{
  "name": "@the-house-protocol/contract",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "bun scripts/deploy.ts",
    "deploy:localhost": "hardhat run scripts/hardhat-deploy.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/hardhat-deploy.ts --network sepolia",
    "deploy:mainnet": "hardhat run scripts/hardhat-deploy.ts --network mainnet",
    "deploy:vault:sepolia": "hardhat run scripts/deploy-vault.ts --network sepolia",
    "deploy:vault:mainnet": "hardhat run scripts/deploy-vault.ts --network mainnet",
    "verify": "hardhat verify --network",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.0.0",
    "dotenv": "^16.4.7",
    "ethers": "^6.13.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.10.0",
    "chai": "^4.5.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0"
  }
}
